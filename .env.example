# Copy to .env for `make run`. Every value has a working default, so the file
# is optional. Nothing but `make run` reads it, and production never does.
HOST=127.0.0.1
PORT=8080
LOG_LEVEL=info
ENV=dev
